import 'package:flutter/material.dart';
import 'hello_view_modal.dart';

class HelloView extends HelloViewModel {
  @override
  Widget build(BuildContext context) {
    return Text('just a placeholder');
  }
}
